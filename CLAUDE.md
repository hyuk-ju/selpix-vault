# CLAUDE.md — Selpix Vault 작업 가이드

> Claude Code가 이 vault에서 작업할 때 읽는 파일입니다.
> 서버 AI(OpenClaw)도 이 vault를 Syncthing으로 실시간 공유합니다.

---

## 이 Vault가 뭔가요?

**셀픽스(Selpix)** 사업의 두 번째 뇌(Second Brain)입니다.

- 도매꾹 소싱 → 쿠팡 자동등록 파이프라인 운영 중
- TikTok 콘텐츠 자동화 (현재 보류)
- AI 에이전트(OpenClaw)가 서버에서 24시간 자동 실행 중
- 이 vault는 사람(사용자) + AI(서버)가 함께 보는 공유 공간

---

## 폴더 구조

```
vault/
├── 📊 대시보드.md          ← 전체 현황 한눈에 (여기서 시작)
│
├── Projects/               ← 진행 중인 프로젝트
│   ├── 셀픽스-쿠팡-파이프라인.md
│   └── TikTok-콘텐츠-자동화.md
│
├── Research/               ← 조사/분석 자료
│   ├── 소싱-트렌드-분석.md
│   ├── 비즈니스-방향.md
│   └── (새 조사 자료 여기에)
│
├── Business/               ← 사업 계획, 수익 계산, 전략 문서
│   └── (사업 관련 파일 여기에)
│
├── Finance/                ← 수익/비용/마진 계산
│   └── (수익 계산서, 정산 자료 여기에)
│
├── Sourcing/               ← 소싱 조사 자료
│   └── (도매꾹/소싱 관련 자료 여기에)
│
├── History/                ← 과거 기록, 타임라인
│   └── 지금까지-한-일.md
│
├── Tasks/                  ← 할일 관리
│   └── 할일-보드.md
│
└── Memory/                 ← AI + 사용자 장기 기억
    ├── Daily/              ← 날짜별 일지
    ├── 운영-규칙.md
    └── 장기기억.md
```

---

## 파일 정리 규칙

### 파일명 형식
- 한국어 사용, 단어 사이 `-` 연결
- 날짜 포함 시: `YYYYMMDD-파일명.md`
- 예: `20260219-쿠팡-수익-분석.md`

### 파일 타입별 처리

| 입력 파일 | 처리 방법 |
|----------|----------|
| Excel / CSV | Markdown 테이블로 변환 + 핵심 인사이트 요약 |
| Word / 한글 | 내용 요약 + 구조화된 Markdown 변환 |
| PDF | 핵심 내용 추출 + 요약 |
| 메모/텍스트 | 구조화하여 적절한 폴더에 저장 |
| 이미지/스크린샷 | `Assets/` 폴더에 저장 + 관련 MD 파일에 링크 |
| 카카오톡 내보내기 | 핵심 결정사항/정보만 추출 |

### Markdown 문서 기본 구조
```markdown
# 제목

> 한 줄 요약

## 핵심 내용
- 포인트 1
- 포인트 2

## 세부 내용
(내용)

## 관련 문서
- [[연결된-파일]]
```

---

## 어디에 넣을지 판단 기준

| 파일 내용 | 저장 위치 |
|----------|----------|
| 쿠팡 파이프라인 관련 | `Projects/셀픽스-쿠팡-파이프라인.md` 업데이트 또는 하위 파일 |
| 소싱 조사, 상품 후보 | `Sourcing/` 또는 `Research/소싱-트렌드-분석.md` |
| 수익 계산, 마진 분석 | `Finance/` |
| 사업 계획, 전략 | `Business/` |
| TikTok 관련 | `Projects/TikTok-콘텐츠-자동화.md` |
| 할일, 체크리스트 | `Tasks/할일-보드.md` |
| 과거 기록, 이력 | `History/` |
| 규칙, 운영 방침 | `Memory/운영-규칙.md` |

---

## 작업 후 반드시 할 것

1. `📊 대시보드.md` — 새 파일 링크 추가 (필요 시)
2. `Tasks/할일-보드.md` — 완료 항목 이동, 새 할일 추가
3. 파일 추가/수정 후 git commit (서버 AI가 자동 push도 함)

---

## 현재 사업 현황 요약

- **쿠팡 등록 상품**: approved 11건 + registered 11건 = 22건 판매 중
- **자동화 크론**: 품절감지(6h) / SEO최적화(4h) / 일일브리핑(9시KST) 운영 중
- **TikTok**: 보류 중 (OpenAI API 발급 후 재개 예정)
- **Obsidian ↔ 서버**: Syncthing 실시간 동기화 (1~2초)

---

## 연락 구조

```
사용자 (Mac)
    ↕ Syncthing
서버 AI (OpenClaw) — Telegram으로 대화
    ↕ 지시
장(ops-dev) / 탐(trend-scout) / 달(worker) — 하위 에이전트
```

궁금한 것은 Telegram에서 서버 AI에게 물어보면 됩니다.
