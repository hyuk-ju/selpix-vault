---
type: changelog
status: active
tags: [system, changelog, operations]
last_updated: 2026-02-21
---
# 시스템 변경 로그

> CLAUDE.md, AGENTS.md, 크론잡, 파이프라인, 스크립트 등 시스템 변경 시 자동 기록.
> 기록 기준: [[Memory/운영-규칙#시스템 설정/이슈 → vault 자동 기록|운영 규칙]] 참조.

---

### 2026-02-22 16:00 — 윈도우 PC Browser Relay 세팅 완료
- **변경**: 윈도우 PC 초기화 → Chrome + Browser Relay + OpenClaw Gateway + Tailscale + WSL2 + WoL 전부 세팅
- **사유**: 서버에서 윈도우 크롬을 원격 제어하여 WAF 있는 사이트(쿠팡 등) 크롤링/자동화
- **영향**: 서버 OpenClaw이 Tailscale Serve(wss://)로 윈도우 크롬 원격 제어 가능. 쿠팡 WAF 우회 확인.
- **핵심 설정**: Tailscale Serve(`desktop-ir43sud.tail39cdea.ts.net`), Gateway bind=lan, 로컬빌드 확장, 디바이스 페어링 완료
- **롤백**: 윈도우 Gateway 중지(`openclaw gateway stop`), Tailscale Serve 끄기(`tailscale serve --https=443 off`)

### 2026-02-22 05:25 — 팀 버스 + 디스패처 시스템 구현 (Phase 1+2)
- **변경**:
  - `lib/team_registry.js` 신규 — 7인 팀 정의, SCRIPT_OWNER, reportStatus()
  - `lib/team_bus.js` 신규 — 에이전트 간 비동기 메시지 큐 (postMessage/getMessages/checkInbox)
  - `team_dispatcher.js` 신규 — 15분 크론 디스패처 (pending→delivered, urgent 텔레그램 알림)
  - `lib/telegram.js` — SENDER_MAP을 team_registry의 SCRIPT_SENDER_TAGS에서 import
  - 7개 크론 스크립트에 reportStatus() + checkInbox() 추가
  - `system_auditor.js` — 팀 상태 보드, dispatchFindings() (감사결과→담당자 자동 라우팅)
  - IF-08 규칙 개선: 좀비/과다 분리, antigravity/IDE 프로세스 예외, 임계값 15→20
  - checkInbox 우선순위 정렬 `||` → `??` 버그 수정 (0이 falsy)
- **사유**: SOUL.md 문서상 팀을 코드로 동작하는 팀으로 전환. 에이전트 간 가시성+소통 인프라 구축
- **영향**: scripts/lib/ 2개 신규, scripts/ 1개 신규, 기존 스크립트 8개 수정, data/team_status.json + team_bus.json 자동생성
- **롤백**: `git log --oneline -5`로 커밋 확인 후 revert

### 2026-02-21 23:30 — AI 브라우저 에이전트 조사 문서 추가
- **변경**: `Research/AI-브라우저-에이전트-조사.md` 신규 생성
- **사유**: AI 자율 브라우저 에이전트 도구 비교 조사 (Browser Use, Skyvern, Computer Use, Operator 등 8개 도구)
- **영향**: vault/Research/ 디렉토리에 새 파일 추가
- **롤백**: `git rm vault/Research/AI-브라우저-에이전트-조사.md`

### 2026-02-21 23:00 — vault-health-check 크론잡 등록
- **변경**: jobs.json에 vault-health-check 크론 추가 (auditor, 매월 1일 06:00 KST, topic:28 알림)
- **사유**: vault_health_check.js가 크론 미등록 상태였음. auditor가 시스템+지식 건강 모두 담당하는 구조 완성
- **영향**: cron/jobs.json, auditor 에이전트 업무 추가
- **롤백**: jobs.json에서 vault-health-check 잡 항목 삭제

### 2026-02-21 22:00 — 멀티에이전트 v2 리팩토링 문서 동기화
- **변경**: AGENTS.md 4곳 수정 (critic→tester 반영, 9→7에이전트 테이블), biz-writer SOUL.md 위임 경로 수정 (main 경유), idea-lab-loop 크론 프롬프트 critic→tester spawn 수정
- **사유**: 9→7 에이전트 리팩토링 후 AGENTS.md/jobs.json/SOUL.md 간 불일치 해소
- **영향**: AGENTS.md, workspace-biz/SOUL.md, cron/jobs.json (idea-lab-loop)
- **롤백**: git checkout HEAD~1 -- config/workspace/AGENTS.md config/workspace-biz/SOUL.md config/cron/jobs.json

### 2026-02-21 12:30 — vault_health_check.js 확장 (Phase 13 전체 검증)

- **변경**:
  - **스캔 범위 확대**: `Research/Ideas/` only → vault 전체 재귀 스캔 (6개 디렉토리 제외: Memory/Daily·Weekly·Monthly, Assets, .git, .obsidian, 00_Vault_Index, .stfolder)
  - **체크 항목 8개**: VH-01(frontmatter 없음→삽입), VH-02(type 유효성), VH-03(confidence 없음→low패치), VH-04(source_agent 없음), VH-05(permanent 90일+ 미리뷰→review-pending패치), VH-06(중복 타이틀), VH-07(created 없음→mtime패치), VH-08(sources 비어있음)
  - **system_auditor 호환**: `finding()` 동일 구조 (id, subsystem, severity, title, detail, file, remediation, fixed, fixResult)
  - **이력 관리**: `data/vault_health_findings.json` + `data/vault_health_history.json` (90일 보관, `saveJsonSafe` 사용)
  - **CLI 옵션 3개**: `--dry-run`(수정 없이 스캔), `--status`(최신 findings), `--history`(90일 추이)
  - **안전 장치**: SKIP_TYPES(changelog/template/index 타입 무시), SKIP_FILES(CLAUDE.md/README.md 등 시스템 파일 스킵)
  - **초회 실행 결과**: 89파일 스캔 → 133건 발견 → 85건 자동수정 (VH-01: 53건, VH-03: 26건, VH-07: 6건)
- **사유**: 기존 스크립트가 Ideas 디렉토리만 스캔, frontmatter 8개 필드 중 2개만 검증, 이력 없음
- **영향**: vault_health_check.js (176줄→421줄), vault 전체 .md 파일의 frontmatter (자동수정 대상)
- **롤백**: git revert vault_health_check.js + data/vault_health_*.json 삭제 + vault git에서 frontmatter 패치 revert

### 2026-02-22 — 아이디어 실험실 2축 모드 시스템 (depth × autonomy)

- **변경**:
  - **핵심 교체**: 3버튼(Speed/Balance/Deep) + type 기반 Fast Track → 2축(depth × autonomy) 시스템
  - **depth 축**: scan(tech만) / validate(trend+tech+critic) / deep(4단계) / auto(4단계+조기종료)
  - **autonomy 축**: auto(자율 분석, 반대→자동패스, 추천→자동사업계획서) / manual(매 단계 사용자 대기)
  - **버튼**: `[🤖 자율 분석] [👤 내가 볼게] [⏳ 대기열] [❌ 기각]`, "내가 볼게" 시 `[🔍 스캔] [⚖️ 검증] [🧠 심층]`
  - **결과 후 버튼**: depth별 분기 (scan→업그레이드, validate→업그레이드, deep→사업계획서/코딩)
  - **하위호환**: 기존 세션의 depth 자동 추론 (isLite→scan, openclaw+skipped→validate, else→deep)
  - **수정 파일 (9개)**: run_idea_discussion.js, process_idea.js, save_idea.js, escalate_idea.js, list_pending_ideas.js, AGENTS.md, jobs.json(idea-lab-loop), test_telegram_messages.js, update_idea_dashboard.js
  - **삭제 기능**: finalizeLiteCard, deepfastIdea, --lite, --deepfast CLI, idea:spd/bal/deepfast 콜백
  - **신규 기능**: upgradeSession (깊이 업그레이드), showDepthButtons, buildInitialPhases(depth) 헬퍼
  - **신규 세션 필드**: depth, autonomy, source
  - **신규 콜백**: idea:auto, idea:manual, idea:scan, idea:validate, idea:upgrade
- **사유**: 기존 3버튼이 실질적 차이 없고, type===openclaw에 의한 Fast Track이 사용자 의도와 무관하게 분석 깊이 결정
- **영향**: 아이디어 실험실 전체 (저장→승인→토론→finalize→업그레이드), AGENTS.md 콜백 지침, idea-lab-loop 크론
- **롤백**: git에서 9개 파일 revert + 기존 세션 JSON의 isLite/computeProfile 필드 복원

### 2026-02-21 21:00 — OpenClaw 팀 체계화 + 리뷰 게이트

- **변경**:
  - **팀 구조 도입**: 6인→9인 (tester, analyst, monitor 신규). ops-dev→CTO, biz-writer→CMO, auditor→감사팀장
  - **openclaw.json**: 3개 에이전트 추가 + 위임 구조 변경 (ops-dev→[worker,tester], biz-writer→[trend-scout,analyst], auditor→[ops-dev,monitor])
  - **워크스페이스 3개**: workspace-tester/, workspace-analyst/, workspace-monitor/ (SOUL.md + HEARTBEAT.md + memory/)
  - **의사결정 로그**: `scripts/lib/decision_log.js` 신규 모듈 + `data/decision_log.json`
  - **리뷰 게이트 삽입**: apply_intel.js, run_coding_execution.js, run_idea_discussion.js에 decision_log 연동 (fail-open)
  - **critic 재토론**: run_idea_discussion.js에 `requestRetry` 필드 지원 (최대 1회)
  - **감사 확장**: system_auditor.js에 'review' 서브시스템 추가 (RG-01: 72h+ 미리뷰, RG-02: 거절 미해결)
  - **문서 업데이트**: SOUL.md(팀구조), AGENTS.md(리뷰게이트+신규에이전트), SYSTEM_STATE.md(9인+위임구조), CLAUDE.md
  - **Claude Code 팀메이트**: `.claude/agents/critic.md`(악마의변호인), `.claude/agents/doc-sync.md`(문서동기화)
- **사유**: 에이전트 간 피어 리뷰 없음, 의사결정 비판적 검토 없음, 에이전트끼리 직접 대화 안 함
- **영향**: 전 에이전트 위임 구조, 인텔/코딩/아이디어 스크립트, 감사 엔진
- **롤백**: openclaw.json에서 tester/analyst/monitor 삭제 + allowAgents 원복 + workspace-* 3개 삭제 + 각 스크립트에서 decision_log 호출부 제거 + system_auditor.js에서 review 서브시스템 제거

### 2026-02-21 21:45 — Critic WARNING 잔여 수정 (W-4, W-6)

- **변경**:
  - **W-4**: `apply_intel.js` — `reviewRequired`를 `APPROVAL_REQUIRED.has(action)` → `risk !== 'low'` 기반으로 변경. medium/high risk 의사결정이 리뷰 대상이 됨
  - **W-6**: `run_coding_execution.js` — `failStep()`과 `skipStep()`에 decision_log 기록 추가. blocked 실패는 medium risk + auditor 리뷰, skip은 low risk + auditor 리뷰
- **사유**: critic 리뷰 W-4(reviewRequired 판정 불완전), W-6(fail/skip 미기록) 지적
- **영향**: apply_intel.js, run_coding_execution.js
- **롤백**: 해당 라인 제거/원복

### 2026-02-21 21:30 — Critic 리뷰 후 수정 (CONDITIONAL PASS → PASS)

- **변경**:
  - **decision_log.js**: raw `fs.writeFileSync` → `json_safe.js`의 `loadJsonSafe`/`saveJsonSafe`/`withLock` 적용 (레이스컨디션 해결)
  - **decision_log.js**: `logDecision()`의 `slice(-MAX_ENTRIES)` → pending 리뷰 항목 보호 로직 추가
  - **decision_log.js**: `resolveRejection(id, note)` API 추가 (거절→해결 전환, RG-02 무한 누적 방지)
  - **monitor 이모지**: `📡` → `🖥️` (trend-scout과 중복 해소). openclaw.json, SOUL.md, AGENTS.md, SYSTEM_STATE.md 동기화
- **사유**: critic 리뷰에서 C-1(레이스컨디션), W-5(resolved 프로토콜 없음), W-7(pending 삭제), W-2(이모지 중복) 지적
- **영향**: decision_log.js 모듈, 이를 사용하는 3개 스크립트(간접), 문서 4곳
- **롤백**: decision_log.js를 이전 버전으로 교체, monitor 이모지 `📡`로 원복

### 2026-02-21 17:00 — 시스템 감사실 에이전트 추가

- **변경**: OpenClaw에 시스템 감사실(System Auditor) 에이전트 추가
  - `scripts/system_auditor.js` 신규 — 38개 체크 × 6개 서브시스템 (쿠팡/크론/아이디어/코딩/인텔/인프라), 자동복구 + 교차분석 + 에스컬레이션
  - `openclaw.json`에 `auditor` 에이전트 추가 (spark 모델, filesystem 스킬)
  - `jobs.json`에 크론 2개: system-audit-periodic (매4h), system-audit-deep (매일05:00 KST)
  - `workspace-auditor/` 생성 (SOUL.md, AGENTS.md, HEARTBEAT.md, memory/)
  - AGENTS.md, SYSTEM_STATE.md, CLAUDE.md 문서 업데이트
- **사유**: pipeline_health_check.js가 쿠팡만 커버하여 크론/아이디어/코딩/인텔/인프라 모니터링 공백
- **영향**: 전 서브시스템 자동 감사, 자동복구(에러리셋/seoTimedOut/lock삭제/로그정리 등)
- **롤백**: openclaw.json에서 auditor 에이전트 삭제 + jobs.json에서 system-audit-* 비활성화 + workspace-auditor/ 삭제

### 2026-02-21 — Selpix SaaS 우선순위 5건 수정 + Vercel 배포

- **배포**: `vercel --prod --yes`로 https://selpix.io 프로덕션 배포 완료
- **배포 방식**: Git 리모트 없이 Vercel CLI 직접 배포 (`.vercel/project.json` 링크)
- **커밋**: `2a98387` — 66 files changed, 4799 insertions(+), 1142 deletions(-)

### 2026-02-21 — Selpix SaaS 코드리뷰 우선순위 5건 수정 (코드리뷰 52점→개선)

- **변경**:
  - **P1 크레딧 레이스컨디션 수정**: `credits.ts`의 `deductCredit()`을 원자적 SQL UPDATE로 교체, `refundCredit()` 추가. AI API 5개 라우트 모두 선차감→실패시환불 패턴으로 전환. `quota-atomic.ts` 신규 — Serializable 트랜잭션으로 일일쿼터 원자적 체크+증분
  - **P2 XSS 취약점 수정**: `step-preview-edit.tsx`에 `isomorphic-dompurify` 적용 (dangerouslySetInnerHTML 새니타이즈). `html.ts` 신규 — escapeHtml 유틸. `payload-builder.ts`의 generateDetailHtml에 escapeHtml 적용
  - **P3 웹훅 핸들러 구현**: Clerk 웹훅 — user.created(upsert+30크레딧), user.updated(이메일/이름 동기화), user.deleted(로그). LemonSqueezy 웹훅 — subscription_created/updated/cancelled 처리, mapLsStatus 헬퍼
  - **P4 컴포넌트 분리**: register-dialog.tsx 853줄 → 7개 파일로 분리 (types/register.ts, hooks/use-register-wizard.ts, 4개 스텝 컴포넌트). 외부 인터페이스 변경 없음
  - **P5 미사용 의존성 제거**: undici 삭제, isomorphic-dompurify 추가
- **사유**: 코드리뷰 점수 52/100 — 보안 취약점, 레이스컨디션, 미구현 스텁, 거대 컴포넌트 지적
- **영향**: AI 크레딧 차감 로직, 쿠팡 등록 쿼터, 웹훅 사용자 동기화, 등록 다이얼로그 UI
- **롤백**: `git stash` 또는 각 파일 개별 revert. 신규 파일: `src/lib/utils/html.ts`, `src/lib/utils/quota-atomic.ts`, `src/types/register.ts`, `src/hooks/use-register-wizard.ts`, `src/components/listing/register-step-*.tsx`

### 2026-02-21 — 보안/품질 이슈 7건 수정

- **변경**:
  - `lib/json_safe.js` 신규 — 원자적 JSON 쓰기 + .bak 백업/복구 + 파일 락
  - `lib/id_utils.js` 신규 — validateId(hex 검증), sanitizeProjectName(경로이탈 방지)
  - `process_idea.js` — execSync→execFileSync(인젝션 차단), json_safe 적용, withLock 적용
  - `run_idea_discussion.js` — execSync→execFileSync, json_safe 적용, skipped 진행률 수정
  - `trigger_bkit_coding.js` — sanitizeProjectName 적용
  - `run_coding_execution.js` — sanitizeProjectName 적용
  - `process_approval.js` — 적용 실패 시 status 'approved'→'apply_failed' 수정
  - `save_idea.js` — execFileSync + json_safe 적용
  - `manage_tasks.js` — json_safe 적용
  - `update_idea_dashboard.js` — skipped 카운트/프로그레스바(⏭️) 수정
  - `google_calendar.js` — 종일 이벤트 end date nextDay() 수정
- **사유**: 코드 감사 발견 — Critical 1(커맨드 인젝션), High 2(경로이탈, 레이스 컨디션), Medium 2(상태 오기록, JSON 손상), Low 2(진행률 불일치, 캘린더 날짜)
- **영향**: 아이디어 실험실 전체 스크립트, 할일 관리, 캘린더 연동
- **롤백**: git revert 또는 lib/json_safe.js·id_utils.js 삭제 + 각 파일의 require/호출부 원복

### 2026-02-21 07:30 — 자동/수동 코딩 모드 선택 + Fast Track 코딩

- **변경**:
  - trigger_bkit_coding.js — 자동 --init 호출 제거, sendWithButtons로 자동/수동 선택 버튼 전송
  - run_coding_execution.js — --manual 명령 추가 (coding_manual 상태 + 텔레그램 안내)
  - run_coding_execution.js — --init에 Fast Track 분기 추가 (openclaw 타입: 초기화/DB/README 생략, 기존 코드 분석 스텝 추가)
  - coding_steps.json에 mode: "fast"/"full" 필드 추가
  - AGENTS.md — coding:auto / coding:manual 콜백 문서화
- **사유**: 자동 코딩이 무조건 시작되어 사용자 선택권 없음 → 텔레그램 버튼으로 모드 선택. openclaw 타입은 기존 시스템 수정이므로 불필요한 스텝 제거.
- **영향**: trigger_bkit_coding.js, run_coding_execution.js, AGENTS.md
- **롤백**: git revert 또는 trigger_bkit_coding.js에 --init 호출 복원 + run_coding_execution.js에서 --manual/Fast Track 분기 제거

### 2026-02-21 06:54 — 자동 코딩 실행 파이프라인 추가

- **변경**:
  - run_coding_execution.js 신규 — coding_spec → 스텝 분해 → 자동 코딩 오케스트레이션
  - trigger_bkit_coding.js 수정 — --trigger 끝에 --init 호출 추가 + 텔레그램 메시지 변경
  - coding-execution-loop 크론잡 추가 (매 2시간, ops-dev spawn)
  - CLI: --init, --list-active, --next, --instruction, --complete, --fail, --skip, --status
  - coding_steps.json 스키마: 프로젝트/스텝/상태/재시도/수용기준 관리
  - 텔레그램 알림: 진행/완료/실패/차단 상태별 메시지 + 인라인 버튼
- **사유**: 코딩 착수 버튼 후 수동 CLI 실행 필요 → 에이전트 자동 코딩으로 전환
- **영향**: trigger_bkit_coding.js, run_coding_execution.js(신규), jobs.json, CLAUDE.md, workspace-dev/tasks/
- **롤백**: jobs.json에서 coding-execution-loop enabled: false + trigger_bkit_coding.js에서 --init 호출 부분 제거

### 2026-02-21 06:20 — 토픽 분리 + Google Calendar 연동 완료

- **변경**:
  - 텔레그램 전용 토픽 생성: 282(아이디어 대시보드), 283(할일 보드)
  - Google Calendar API 모듈 신규 (`lib/google_calendar.js`) — 서비스 계정 JWT, 외부 npm 없음
  - manage_tasks.js에 캘린더 자동 연동: add→이벤트 생성, done→완료표시, cancel→삭제
  - `--gcal-sync` 명령어 추가 (전체 동기화)
  - todo-board-daily-sync 크론에 `--gcal-sync` 추가 (매일 09:00 KST)
  - .env에 GOOGLE_SA_KEY_PATH, GOOGLE_CALENDAR_ID, 토픽 ID 추가
- **사유**: 할일 마감일을 Google Calendar에서 관리 + 대시보드/보드 전용 토픽 분리
- **영향**: telegram.js(TOPIC 추가), manage_tasks.js(gcal 연동), update_idea_dashboard.js(토픽 변경), dashboard_state.json(토픽 ID 변경), jobs.json(크론 메시지 수정), AGENTS.md, SYSTEM_STATE.md, CLAUDE.md
- **롤백**: .env에서 GOOGLE_* 삭제 → gcal 자동 스킵 / 토픽 변수 삭제 → 기존 토픽으로 폴백

### 2026-02-21 02:00 — 아이디어→사업계획서→코딩 파이프라인 추가
- **변경**: 5-Phase 깔때기 완성 — 사업계획서 자동 생성(generate_business_plan.js) + bkit PDCA 코딩 트리거(trigger_bkit_coding.js) + 관련 스크립트 확장
- **사유**: 아이디어 승인 후 시장조사/사업계획서 자동 생성 → 토론 → 코딩 착수까지 자동화
- **영향**: generate_business_plan.js(신규), trigger_bkit_coding.js(신규), run_idea_discussion.js(버튼변경+bizplan-discuss/code), process_idea.js(--bizplan/--bizplan-discuss/--code), idea-lab-loop 크론잡(bizplan처리), AGENTS.md(콜백추가), SYSTEM_STATE.md, CLAUDE.md
- **롤백**: 신규 스크립트 2개 삭제 + run_idea_discussion.js/process_idea.js 버튼/CLI 원복 + jobs.json 메시지 원복

### 2026-02-20 18:30 — 새 기능 추가
- **변경**: 아이디어 실험실 시스템 구축 (save_idea.js, process_idea.js, run_idea_discussion.js)
- **사유**: 일일 브리핑에서 나온 수익화 아이디어를 체계적으로 검증하기 위해
- **영향**: AGENTS.md (콜백 처리 추가), jobs.json (daily-briefing 프롬프트 수정), .env (토픽 상수), telegram.js (sendWithButtons)
- **롤백**: 스크립트 3개 삭제 + AGENTS.md 아이디어 섹션 제거 + .env 토픽 변수 삭제

### 2026-02-20 18:35 — 아이디어 실험실 확장
- **변경**: 4번째 에이전트 "악마의 변호인(critic)" 추가. 3인 분석 → 4단계 분석
- **사유**: 에이전트들의 찬성 편향 방지. 의도적으로 반대 근거를 찾는 역할 필요
- **영향**: run_idea_discussion.js (critic phase + verdict downgrade), AGENTS.md (Step 4.5), vault 운영 규칙
- **롤백**: run_idea_discussion.js에서 critic phase 제거 + TOTAL_PHASES=3으로 복원

### 2026-02-20 18:36 — E2E 테스트 통과
- **변경**: mock 데이터로 전체 플로우 테스트 (save → approve → init → 4 phase → finalize → complete → 큐 자동 시작)
- **사유**: 실제 운영 전 검증
- **영향**: 없음 (테스트 데이터 정리 완료)
- **롤백**: 해당 없음

### 2026-02-20 18:38 — process_idea.js 버그 수정
- **변경**: 텔레그램 시작 메시지 "3명의 AI" → "4단계 AI 분석", 에이전트 순서도 정확하게 수정
- **사유**: critic 추가 후 메시지가 구버전 그대로였음
- **영향**: process_idea.js 시작 알림 메시지
- **롤백**: 문자열 복원

### 2026-02-20 18:40 — vault 변경 기록 시스템 구축
- **변경**: 변경로그.md 신규 생성 + 운영-규칙.md에 자동 기록 규칙 테이블 추가 + AGENTS.md에 에이전트용 기록 규칙 추가 + CLAUDE.md에 개발자용 기록 규칙 추가
- **사유**: 중요 시스템 변경/이슈를 vault에 체계적으로 기록하기 위해
- **영향**: CLAUDE.md, AGENTS.md, vault/Memory/운영-규칙.md (3곳에 규칙 추가)
- **롤백**: 각 파일에서 vault 변경 기록 섹션 제거 + 변경로그.md 삭제

### 2026-02-20 18:45 — 문서 동기화 (/sync-docs)
- **변경**: SYSTEM_STATE.md 섹션 7 크론 테이블 전면 교체 (5개 → 20개 잡 반영). 삭제된 잡 3개 제거, 인텔 크론 enabled 상태 수정. CLAUDE.md 코드 구조(13→20개), 파이프라인 흐름(12개 잡 추가), telegram.js 설명, 자주 수정하는 파일에 아이디어 스크립트 추가
- **사유**: jobs.json 실제 상태와 문서 간 큰 괴리 발견
- **영향**: SYSTEM_STATE.md, CLAUDE.md
- **롤백**: git revert

### 2026-02-20 18:54 — openclaw.json 토픽 177 등록
- **변경**: channels.telegram.groups.topics에 토픽 177 추가 + systemPrompt 설정 (아이디어 실험실 역할). 서비스 재시작 완료
- **사유**: 토픽 allowlist 전환 대비 + 에이전트가 177에서 아이디어 실험실 컨텍스트를 인식하도록
- **영향**: openclaw.json, openclaw-gateway.service 재시작
- **롤백**: openclaw.json에서 "177" 키 제거 + 서비스 재시작

### 2026-02-20 19:25 — ADR + Runbook 노트 체계 도입 (Phase A)
- **변경**: vault/ADR/ + vault/Runbooks/ 폴더 생성. 지식 기반 운영 시스템 기획안에서 ADR, Runbook 2종만 우선 적용
- **사유**: 에이전트 토론 결과를 구조화된 결정 기록(ADR)으로 남기기 위해
- **영향**: vault 폴더 구조 (2개 폴더 추가)
- **롤백**: vault/ADR/ + vault/Runbooks/ 삭제

### 2026-02-20 19:28 — Decision Record + ADR 자동 생성 (Phase B)
- **변경**: run_idea_discussion.js에 3가지 추가: (1) critic alternatives 필드, (2) 텔레그램/vault 카드에 Decision Record 섹션, (3) finalize 시 vault/ADR/ADR-{번호}-{제목}.md 자동 생성
- **사유**: 토론 결과를 Decision Record 5필드(결론/가정/대안/리스크/Next Actions) 형태로 규격화
- **영향**: run_idea_discussion.js (buildTelegramCard, buildVaultMarkdown, finalizeSession), AGENTS.md (critic 프롬프트에 alternatives 추가)
- **롤백**: run_idea_discussion.js에서 ADR 생성 블록 제거 + PHASE_SCHEMA critic에서 alternatives 제거

### 2026-02-20 19:30 — Phase B E2E 테스트 통과
- **변경**: mock 데이터로 alternatives 포함 전체 플로우 테스트. ADR-001 자동 생성 + critic 반대 → verdict 하향 정상 확인
- **사유**: Phase B 코드 변경 후 검증
- **영향**: 없음 (테스트 데이터 정리 완료)
- **롤백**: 해당 없음

### 2026-02-20 21:10 — 토픽 분리 + Google Calendar 연동

- **변경**:
  - 텔레그램 토픽 2개 신규 생성 (282=아이디어 대시보드, 283=할일 보드)
  - 대시보드/보드를 전용 토픽으로 이동 (기존 177, 31에서 분리)
  - Google Calendar API 모듈 추가 (`lib/google_calendar.js`) — 서비스 계정 JWT, 외부 의존성 없음
  - manage_tasks.js에 `--gcal-sync` 명령어 + 자동 캘린더 이벤트 생성/완료/삭제
  - .env에 `TELEGRAM_TOPIC_IDEA_DASHBOARD=282`, `TELEGRAM_TOPIC_TODO_BOARD=283` 추가
- **사유**: 아이디어/할일 전용 공간 확보 + 캘린더 마감 관리
- **영향**: 기존 토픽 177/31의 핀 메시지는 더 이상 업데이트 안 됨 (새 토픽으로 이동)
- **롤백**: .env에서 TOPIC_IDEA_DASHBOARD/TODO_BOARD 삭제 → 코드가 기존 토픽으로 폴백

---

## 관련 문서
- [[Memory/운영-규칙|운영 규칙]]
- [[Memory/아이디어-실험실-운영-규칙|아이디어 실험실 운영 규칙]]
- [[📊 대시보드|📊 대시보드]]
